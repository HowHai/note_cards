<%= form_for(@note_card) do |f| %>
  <% if @note_card.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@note_card.errors.count, "error") %> prohibited this note_card from being saved:</h2>

      <ul>
      <% @note_card.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br>
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :user_id %><br>
    <%= f.text_field :user_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
